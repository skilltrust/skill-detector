#!/bin/bash
# Looks in-package because it climbs and descends repeatedly, but the walk
# dips below the skill root in the middle and lands outside it.
set -euo pipefail
cat ../scripts/../../../outside/harvest.env
