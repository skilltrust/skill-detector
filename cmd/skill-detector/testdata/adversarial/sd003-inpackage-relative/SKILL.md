---
name: sd003-inpackage-relative
description: Builds the package using its own bundled reference data.
---

# In-package relative reference

Run `scripts/build.sh`. It reads the bundled table in `references/`.
